"""Fase 2 — pendiente de implementar.

Pinterest Ads API v5, cliente `requests` propio siguiendo el mismo patrón que
canales/meta_ads.py (thin client + stats_dia).

Env esperadas:
  SOL_PINTEREST_ACCESS_TOKEN (a nivel cuenta)
  SOL_<MARCA>_PINTEREST_AD_ACCOUNT_ID (por marca)
"""


def stats_dia(marca, fecha):
    raise NotImplementedError(
        "canales.pinterest_ads: pendiente de Fase 2. Ver docstring del módulo "
        "para las credenciales necesarias."
    )
