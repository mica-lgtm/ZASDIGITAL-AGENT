"""Fase 2 — pendiente de implementar.

Perfit (email marketing) REST API, auth por API key. Los campos normalizados
que aplican acá son distintos a los de un canal pago (sin spend/cpa/roas):
emails enviados, tasa de apertura, tasa de click, conversiones atribuidas.

Env esperadas:
  SOL_<MARCA>_PERFIT_API_KEY (por marca)
"""


def stats_dia(marca, fecha):
    raise NotImplementedError(
        "canales.perfit: pendiente de Fase 2. Ver docstring del módulo "
        "para las credenciales necesarias."
    )
