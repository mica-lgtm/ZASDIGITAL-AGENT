"""Fase 2 — pendiente de implementar.

TikTok Business API (Marketing API), cliente `requests` propio siguiendo el
mismo patrón que canales/meta_ads.py (thin client + stats_dia).

Env esperadas:
  SOL_TIKTOK_ACCESS_TOKEN (a nivel cuenta)
  SOL_<MARCA>_TIKTOK_ADVERTISER_ID (por marca)
"""


def stats_dia(marca, fecha):
    raise NotImplementedError(
        "canales.tiktok_ads: pendiente de Fase 2. Ver docstring del módulo "
        "para las credenciales necesarias."
    )
